@page
@model DrawClient.Pages.Customer.Exam.ListModel

<!--====== PAGE BANNER PART START ======-->

<section id="page-banner" class="pt-105 pb-110 bg_cover" data-overlay="8" style="background-image: url(/images/page-banner-2.jpg)">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="page-banner-cont">
                    <h2>Your Exam Result</h2>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Home</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Exams</li>
                        </ol>
                    </nav>
                </div>  <!-- page banner cont -->
            </div>
        </div> <!-- row -->
    </div> <!-- container -->
</section>

<!--====== PAGE BANNER PART ENDS ======-->

<section id="courses-part" class="pt-90 pb-120 gray-bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div>
                    <div class="row">

                        <table class="table table-bordered table-hover">
                            <thead>
                                <tr class="tbl-exam">
                                    <th>Course Name</th>
                                    <th style="text-align: center;">Result</th>
                                    <th style="text-align: center;">Details</th>
                                </tr>
                            </thead>

                            <tbody>
                                @* @foreach (var coach in Model.Coaches.Items)
                                {
                                <tr>
                                <td style="vertical-align: middle;">@coach.User.Name</td>
                                <td style="vertical-align: middle;">@coach.User.Phone</td>
                                <td style="vertical-align: middle;">@(string.IsNullOrWhiteSpace(coach.User.Email) ? "No data" : coach.User.Email)</td>
                                <td style="vertical-align: middle;">
                                <div class="form-check form-switch m-0 p-0 d-flex justify-content-center">
                                <input class="form-check-input m-auto" type="checkbox" checked="@coach.User.IsBlocked" id="@coach.Id">
                                </div>
                                </td>
                                <td class="text-center">
                                <a class="btn btn-dark" asp-page="Update" asp-route-coachId="@coach.Id">
                                <i class="bi bi-pencil-square"></i>
                                </a>
                                </td>
                                </tr>
                                } *@
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>